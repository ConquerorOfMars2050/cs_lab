#include <stdio.h>

int main (void) {

    unsigned int n;

    printf("Enter a positive integer: ");
    scanf("%u", n);

    int counter = 1;

    int a, b; 
    
    n > b && n > a && b > a;

    while (n >= 1) {

        n = a + b; 
    }

}